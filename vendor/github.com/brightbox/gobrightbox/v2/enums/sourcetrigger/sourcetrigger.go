// Code generated by generate_enum; DO NOT EDIT.

// Package sourcetrigger is an enumeration of the states Manual, Schedule
package sourcetrigger

import (
	"encoding/json"
	"fmt"
	"reflect"
)

// Enum is an enumerated type
type Enum uint8

const (
	// Manual is an enumeration for sourcetrigger.Enum
	Manual Enum = iota + 1
	// Schedule is an enumeration for sourcetrigger.Enum
	Schedule
)

// ValidStrings is the set of strings that are valid inputs to ParseEnum
var ValidStrings = []string{
	Manual.String(),
	Schedule.String(),
}

// String makes Enum satisfy the Stringer interface
func (i Enum) String() string {
	tmp, err := i.MarshalText()
	if err == nil {
		return string(tmp)
	}
	return ""
}

// ParseEnum attempts to convert a string into a Enum
func ParseEnum(name string) (Enum, error) {
	switch name {
	case "manual":
		return Manual, nil
	case "schedule":
		return Schedule, nil
	}
	var zero Enum
	return zero, fmt.Errorf("%s is not a valid sourcetrigger.Enum", name)
}

// MarshalText implements the text marshaller method
func (i Enum) MarshalText() ([]byte, error) {
	switch i {
	case Manual:
		return []byte("manual"), nil
	case Schedule:
		return []byte("schedule"), nil
	}
	return nil, fmt.Errorf("%d is not a valid sourcetrigger.Enum", i)
}

// UnmarshalText implements the text unmarshaller method
func (i *Enum) UnmarshalText(text []byte) error {
	name := string(text)
	tmp, err := ParseEnum(name)
	if err != nil {
		return &json.UnmarshalTypeError{
			Value: name,
			Type:  reflect.TypeOf(*i),
		}
	}
	*i = tmp
	return nil
}
